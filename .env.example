# Para desarrollo local usa SQLite:
DATABASE_URL=sqlite:///biblioteca.db
ENV=dev

# Para despliegue usa PostgreSQL, por ejemplo Neon:
# DATABASE_URL=postgresql://usuario:password@host/database?sslmode=require
